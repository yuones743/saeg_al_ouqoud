workflows:
  android-release:
    name: Android Release
    environment:
      flutter: stable
    scripts:
      - rm -f pubspec.lock
      - flutter pub get
      - flutter build apk
    artifacts:
      - build/app/outputs/flutter-apk/app-release.apk
